#db.dataSourceClassName="org.postgresql.ds.PGSimpleDataSource"
#db.dataSource.databaseName="ledger-cal"
#db.jdbcUrl="jdbc:postgresql://localhost:5432/ledger-cal"

database {
 host="localhost"
 host=${?DB_HOST}
 dbname="ledger-cal"
 username=${POSTGRES_USERNAME}
 password=${POSTGRES_PASSWORD}
}